# 留一格 · 旧物开箱：回忆收纳师

HTML + 原生 JavaScript + Canvas 2D 的可玩版本，1.0.2，2026-09-13。

## 试玩

- 直接用现代浏览器打开 `index.html` 即可；不需要联网或安装依赖。
- 推荐使用本地服务器，保持存档来源稳定：在此目录执行 `node serve.cjs`，打开 `http://127.0.0.1:8871`。
- 鼠标拖放或触屏拖放；也可以先点物品再点目标格。
- 选中后按 R 或点“旋转”；方向键移动预览，Enter 放下，Escape 取消。
- 单击仅选中，只有按住拖动才跟随鼠标；放下、失败或取消后停止跟随。
- 右键点击抽屉里的物品可直接取回桌面；该操作支持撤销。手机使用“放回桌面”按钮。
- 基础收纳、隔板抽屉、分区委托这几章：全部物品放入即可通关，不必填满，空格可以保留。
- 从**留物取舍**章节起难度逐关抬升：物品栏会涨到十几件（最多十八件），并且**通关时最多只允许剩一格**。取舍关同理——但要先挑对「留哪几件」才凑得出格子数，只保证「至少有一种挑法能通关」，挑错可以退回来重挑。
- 「一点提示」每份委托自带一次，用完后看一段激励视频再得一次；「消除一件」同样看广告获得，可以把一件不必须、也没被委托人固定的旧物从这份委托拿掉（每份最多消两件）。算不出可消的件时会直接说明，不会让广告白看。**按钮文案会照实说**：广告位没配好、或不在 TapTap 客户端里时，这几次帮忙是直接免费给的，按钮就只写「帮我放一件」「消除一件」（不写「看广告」），配好之后自动变回「看广告 · …」——游戏内的隐私政策写着「没有接入任何广告 SDK」，两处必须对得上。
- 提示、撤销和重新整理免费。物品可从抽屉移回桌面。
- 通关后可以下载 PNG 作品图，图上带整齐度、分项拆解和图鉴进度。本次是本地导出，并未接入 TapTap 社区分享。
- 安卓手机可以装 `dist/liuyige.apk`（Android 7.0 及以上）。它跑在系统 WebView 里，和浏览器版是同一份文件，玩法完全一致。构建方式见 `node android/tools/build-apk.cjs`。

## 内容规模

100 份委托，分 8 个章节。开局是 7 关的**新手章节**：七种玩法各取一关、按最轻的难度呈现，先把“隔板、分区、取舍、固定件”这些规矩各认一遍，再进入各自的正式章节。前 50 关是**第一辑**，后 50 关是**第二辑**——同一章节里的第二辑关卡按春→冬推进，翻开委托簿能看出时间在走。

| 章节 | 关数 | 玩法 |
|---|---|---|
| 新手章节 | 7 | 七种玩法各一关，规则最轻 |
| 基础收纳 | 13 | 全部放入即可，可以留白 |
| 隔板抽屉 | 13 | 木色斜纹格不可占用 |
| 分区委托 | 13 | 指定物品须完整落在同名标记区 |
| 留物取舍 | 13 | 只能留一部分，配额固定；**从这一章起要求摆到只剩至多一格** |
| 隔板分区 | 14 | 隔板 + 分区叠加，同样要摆到只剩一格 |
| 取舍分区 | 13 | 取舍 + 分区叠加，同样要摆到只剩一格 |
| 大师委托 | 14 | 固定件不可移动，难度最高，同样要摆到只剩一格 |

物品栏件数也是从「留物取舍」正文起逐关抬升的：10 件 → 18 件（旧物一共 18 种，18 是硬顶）。件数多了就靠「每排少放一件、多排一行」把格子撑宽，而不是一味把格子压扁。

### 画布版式：宽屏一套、手机一套

画布是一张**按 CSS 宽度等比缩放的逻辑画布**：屏幕上的尺寸 = 逻辑尺寸 ×（CSS 宽 ÷ 逻辑宽）。所以逻辑尺寸越小，屏幕上的东西越大——版式必须跟着屏幕宽度换。

| | 宽屏（桌面 / 平板） | 手机 |
|---|---|---|
| 逻辑画布 | 640×640（原设计稿，逐项未改） | 400×**内容决定的高度** |
| 缩放比 | 0.85 ~ 1.0 | ≈ 0.92（接近 1 逻辑 px = 1 CSS px） |
| 棋盘 | 居中，两侧各留 ~185 逻辑 px | 顶满可用宽度，格宽 44~53 CSS px |
| 物品栏 | ≤6 件三列 / 7-8 件四列 / 9-10 件五列 / ≥11 件六列（18 件 = 6 列×3 行） | 每排按「一格至少 74 逻辑 px」算：390 屏上 18 件 → 4 列×5 行，视口到 650 那一档是 5 列×4 行 |
| 操作区 | 七个按钮排成一行 | 分两排：上排三个「救援」（117×64），下排四个基础操作（86×48） |
| 页面顺序 | 委托簿在左、游戏在右 | 游戏区提到最前（`.play-area{order:-1}`），委托信与委托簿挪到下面 |

- 版式由 `game.js` 的 `computeLayout(cssW, level)` 一次算清（棋盘、木框、提示语、分隔线、物品栏全套坐标），`render()` 只负责取用，不再有写死的 367 / 380 / 400 这些数字。
- 「是不是手机」**只看视口宽度 ≤650**，和 `style.css` 里那条媒体查询共用同一个断点（页面按手机排、画布却按桌面画就会错配）。这里曾经还留过一条「画布窄于 480 也算手机」的兜底，结果是桌面窗口矮一点（1280×800 的窗口，画布被 max-width 压到 448）就被判成手机，换成一张比窗口还高的长画布——棋盘是大了，物品栏却掉到屏幕外，拖东西要来回滚。对拖拉类玩法来说「一屏看全」比「格子大」重要，所以矮窗口只当是「小一号的桌面」。这条有真实浏览器验收守着（`tools/_verify-mobile.cjs` 第 9 项）。
- 手机版式下画布高 596~981 CSS px（390 屏 733），棋盘越大画布越高，页面滚动是预期行为；拖动与「先点物品再点格子」两种放置方式都成立。
- 改断点或改 `pad/gap/minTile` 之前，先跑 `node tools/layout-sweep.cjs`：它把 13 档屏幕的画布宽、棋盘格、箱子格、物品栏形态、画布高列成一张表，能直接看出曲线连不连续（两套稿的接缝在视口 650，和媒体查询同一条线，切换时整页排布一起变）。

委托簿里**每个章节单独编号**：列表序号是本章内部的顺序（隔板抽屉 13 关就编 01–13），不是全书序号。全书进度由顶栏的「X / 100 已整理」和信头的「87 / 100」表示。早先版本用的是全书序号，切到某一章会看到 13, 14, 15, 22, 23, 24, 52, 59… —— 中间全是被别章占掉的号，读起来像漏了关卡，已改掉。

18 种旧物全部至少在一份委托里出现过；多解：100 关都有两个以上合法答案。

### 难度曲线：从「留物取舍」起要求摆到只剩一格

**留物取舍**这一章的正文从 `drawer-16` 起（`drawer-15` 是该章的教学关，保持轻量），往后到 `drawer-100` 共 **82 关**，全部由 `tools/nearfull.cjs` 重新配过：物品格数总和与抽屉可用格数**恰好对齐**，摆完只剩 0~1 格；物品栏件数从 10 件线性抬到 18 件。前 18 关（含 7 关新手章节）沿用旧的增强表，仍是可以留白的入门难度。

- **不是简单堆件数。** 生成器先定「这份委托要放十到十八件」，再反过来挑抽屉尺寸与追加的旧物，让总和正好落在可用格数上（差 0 或 1）。每一关都要过真实求解器：可解、可完成、节点留足余量，并且**逐件固定后仍可解**——这一条是为「一点提示」准备的，提示要能在玩家已经摆了一半的盘面上继续给。
- **取舍关不能只顾盘面。** 玩家人手只放 `keepCount` 件，「能凑满格数」的挑法本来就少。所以生成器把件数当成未知量，对每块装得下的抽屉反解「挑任意几件恰好凑够」，并要求**至少 6 种挑法能通关**（实测多数在 20 种以上，drawer-47 有 90 种）。只保证有解，挑错可以重挑。
- **棋盘只能放大，不能缩到装不下原有隔板与分区。** 增强表只能覆盖尺寸与追加件，裁掉的隔板／分区在运行时不会跟着消失，所以生成器加了一道闸：新尺寸必须装得下原有的全部隔板与分区格。
- **近满盘对求解器反而更友好。** 约束越强，MRV 剪枝越有效：18 件填满 9×8 只要 590 节点 / 18ms。全量 82 关里最坏的一关也在 6 万节点预算内。

第二辑（第 51–100 关）原先靠**形状别扭度 + 条件叠加**（隔板／分区／取舍同时出现）加难度、件数反而收窄；现在件数上限放开到 18，两套手段叠在一起用。批次装配脚本见 `tools/nearfull.cjs` 与 `tools/apply-nearfull.cjs`，成品规格见 `tools/nearfull-report.txt`。

## 特色系统

**新手章节。** 不按“第 1 关最简单”排序，而是从每种玩法里各抽一关，把规则一次认全。其余关卡解锁后按玩法分章排列。

**整齐度（随摆随算）。** 三部分加权：对齐 45%、留白 30%、居中 25%。

- **对齐**：每件旧物的四条边，能和抽屉边或另一件旧物共线就记一分。
- **留白**：空位应该推到边上或靠住隔板，别在物品中间挖洞。看的是空位的去向，不是空位的数量。
- **居中**：整体不要全堆在一角。

分数只作参考，不设星级门槛——各关的可达上限并不一样（离线实测最低的一关也能爬到 84，最高 100，评语线因此定在 82）。所以给的是**一句评语 + 一条“下一步动什么”的建议**，而不是一个够不到的星星目标。

**回忆图鉴。** 24 段旧物关系，把两件相关的旧物按特定方式摆在一起就会解锁。四种关系：挨在一起、并排、上下摞着、各靠一边。解锁写进本机存档，同一段只记一次。图鉴面板只在第一次打开时构建。

**背景音乐（现场合成，没有音频文件）。** 八小节循环 32 秒，60 BPM：慢起的暖垫（Dmaj9 → Bm7 → Gmaj7 → A7sus4 走两遍）+ 稀疏的音盒主旋律 + 每小节两下的低音。空拍比音符多，是背景不是主角。

- 用 Web Audio 现场合成，**不挂 mp3**。理由有三：这是一个纯静态、零构建、双击就能玩的页面，几 MB 的音频文件会让首屏和离线包都变笨重；合成不占体积，循环处相位连续，没有接头爆音；也不牵扯音乐授权。
- 浏览器要求音频必须由用户手势启动，所以**第一次点画面或按键才开始响**，不在加载时打扰人。
- 右上角「声音」一个开关同时管 BGM 和提示音，选择记进本机存档、下次打开沿用。
- 切到别的标签页会自动淡出，回来再接上。
- 提示音和 BGM 共用同一个 `AudioContext`；关闭时先淡出 1 秒再收声，不会有硬切。

## 文件

| 文件 | 用途 |
|---|---|
| index.html / style.css | 页面、短故事、工具、弹窗与响应式布局 |
| core.js | 物品、关卡数据、章节、旋转、碰撞、多解检查、提示求解器、整齐度评分、旧物关系；无DOM依赖 |
| art.js | Canvas 程序绘制的旧物插画；无需图片下载 |
| music.js | 背景音乐：Web Audio 现场合成的 32 秒循环，不依赖任何音频文件；无 DOM 依赖，没有 AudioContext 时安静降级 |
| ads.js | 激励视频适配层：只回答「这一次机会给不给」。TapTap 小游戏里走 `tap.createRewardedVideoAd`，并且**只在 `onClose(res => res.isEnded)` 为真时发奖励**；网页版／没配广告位时直接放行，不卡玩家。广告位 ID 留空即视为未接入 |
| game.js | 输入、渲染、状态、存档、完成流程、提示与消除额度、图鉴面板、作品导出、声音开关 |
| serve.cjs | 可选的纯Node本地预览服务器 |
| tools/harden.cjs、apply-harden.cjs | 难度增强表的生成与写回；改表后必须重跑校验。生成器读 `K.RAW`（未增强的原始关卡），不读 `K.levels`——否则差值恒为负，整片关卡会被跳过 |
| tools/nearfull.cjs、apply-nearfull.cjs、nearfull-verify.cjs | 近满盘生成器：把 `drawer-16` 起的 82 关改成「通关最多空一格」，并把物品栏件数抬到 10~18 件。合并写回（不整体替换），前面的关卡继续用旧表。**生成器读的是 `K.RAW` 快照，成品必须再按 `K.levels` 验一遍**——快照与运行时之间还夹着 HARDEN 和关系补丁，差一件物品结论就不成立 |
| tools/fix-letters.cjs | 一次性文案修正：把取舍关信件与标题里写死的件数（「另外三件你替我挑」）改成不依赖数字的写法。配额被上调之后这些数字会自相矛盾，而规则卡本来就会写「从 N 件中选留 K 件」 |
| tools/ceiling.cjs、gradient.cjs、dims.cjs、spread.cjs、multisol.cjs、relations-check.cjs、baseline.cjs | 分数上限、整齐度梯度、维度分布、分数跨度、多解、关系可达性、关卡结构基线的离线校验 |
| tools/shots.cjs | 无头 Chrome 验收截图 |
| tools/pack-taptap.cjs | 打 TapTap 上传包：只挑运行时文件铺进 `dist/liuyige/`，再写成结构合规的 zip，并逐项自检（第一级唯一文件夹、入口在第二层、无反斜杠、解压回来与源文件逐字节一致）。自己写 zip 而不用 `Compress-Archive`，因为后者历史上会用反斜杠当条目分隔符 |
| tools/bump-version.cjs | 版本升档：当前版本号散在 8 个文件里（README / TAPTAP_UPLOAD / TEST_REPORT / DEVELOPMENT_PLAN / NEXT_VERSION_PROPOSAL / android/README / pack-taptap / build-apk），这个脚本一次改齐。每条替换要求精确命中恰好 1 次，对不上就整批中止；**历史小节标题不在表里**。`versionCode` 必须严格大于旧值，否则新包装不上去 |
| android/ | 安卓壳工程：一个 WebView 承载**同一份**游戏文件，产出 `dist/liuyige.apk`。不用 Gradle，直接调 build-tools 的 `aapt2 → javac → d8 → zipalign → apksigner`。壳里唯一一个 Java 文件只做三件事：把 WebView 配成能跑 Canvas + localStorage + Web Audio、载入 `assets/liuyige/`、跟随生命周期。构建方式与踩坑记录见 `android/README.md` |
| android/tools/verify-apk.cjs | APK 产物校验：拆开包核对框架文件、条目名分隔符、7 个游戏文件与源文件逐字节一致、签名有效、没有把测试或文档打进去 |
| tools/_verify-mobile.cjs | 真浏览器验收手机版式（35 项）：切到窄屏版式、量棋盘格 / 箱子格 / 每排件数、七个按钮可见且在两排、游戏区排到最前、真 PointerEvent 点选放置、320 窄屏复验、**桌面矮窗口仍是原设计稿**、全程无 JS 报错。假 DOM 能核对数字，但「按钮在手机上真的在两排吗」「有没有横向溢出」只有真浏览器算数 |
| tools/overflow.cjs | 窄视口体检：在 12 个宽度下用 **iframe 造真实视口**渲染，报出 `scrollWidth` 与每个越界元素，并可另存窄视口截图。之所以用 iframe，是因为无头 Chrome 的 `--window-size` 有 500px 下限，直接开窄窗口只会得到「按 500 排版再裁图」的假象 |
| tools/layout-sweep.cjs | 版式对照表：13 档屏幕各是多大（画布宽 / 棋盘格 / 箱子格 / 物品栏形态 / 画布高 / 横向余量），并把两套稿接缝处的跳变幅度量出来。数字全部问 `GameDebug.layout()` 要，不另抄一份公式——所以「工具说没问题、游戏里其实不是这样」不会发生 |
| promo/ | TapTap 宣传片的分镜（`promo.html` / `promo.css` / `promo.js`）：为「不得与实机视频高度雷同」另写的一套 16 秒横屏动画，复用 `art.js` 的旧物画法与 `core.js` 里每件旧物自己的记忆文案，但全程没有指针、按钮、状态栏和操作过程 |
| tools/make-promo.cjs、promo-preview.cjs | 录宣传片并自检 22 项。`promo-preview.cjs` 只渲染若干时间点的画面、不录制——改分镜先跑它，比等 16 秒重录快得多。自检分两层「看成品」：① 自己拆 MP4 盒子；② 让 Chrome 真播一遍这个文件、读元数据并 seek 出 5 张截图（解析盒子证明不了「真的能解码」） |
| tools/music-check.cjs | 背景音乐离线体检：用假 AudioContext 推着调度器走完两个循环，核对每小节的和弦数、循环有没有绕回去、音高与包络是否合法 |
| tools/music-probe.html | 真实浏览器探针：确认真实的 ConvolverNode / DynamicsCompressor / 指数斜坡都建得起来（假 context 证不了这一半） |
| tests/*.test.cjs | 单元与回归测试（见下） |
| DEVELOPMENT_PLAN.md | 画风、技术方案、范围、排期与 TapTap 发布路线 |
| NEXT_VERSION_PROPOSAL.md | 下一版候选功能与取舍 |
| TAPTAP_UPLOAD.md | 上传包结构、自检说明、三条承载路线的取舍、控制台字段、物料清单与上传前检查清单 |

## 宣传片（TapTap 首页推荐物料）

`dist/liuyige-promo.mp4` —— 由 `node tools/make-promo.cjs` 现场生成，不是实机录屏。

| 项 | 值 |
|---|---|
| 时长 | 16.0s（判据是「不少于 15s」，见下） |
| 画面 | 1920×1080，16:9，30fps |
| 编码 | H.264（`avc1.640033`）+ AAC 音轨 |
| 体积 | 6.73 MB（上限 5GB） |

五场分镜：旧物浮现 → 九件归位 → 特写蒙太奇（相机 / 耳机 / 来信，各配一句它自己的记忆）→ 合箱只剩一格 → 品牌落版。
背景音乐是 Web Audio 现场合成的，和游戏里同一套写法，不挂音频文件。

**为什么是 16 秒**：判据是「不少于 15s」，而录出来正好 15.000 就卡在边界上——MediaRecorder 的时间戳取整、
播放器按时长四舍五入，都可能被读成 14.99。末场定格多留一秒，余量就出来了。要正好 15 秒的话，
把 `promo/promo.js` 里的 `TOTAL` 和落版那场的 `dur` 一起调回去、重录一次即可。

**为什么不用 ffmpeg**：这台机器上没有 ffmpeg。Chrome 126+ 的 MediaRecorder 支持 `video/mp4;codecs=avc1`，
所以走 `canvas.captureStream(30)` 配 Web Audio 的 `MediaStreamDestination` 直接出 MP4。
代价是录一次要等一个完整的 16 秒实时时长，所以改分镜先跑 `tools/promo-preview.cjs` 看画面。

## 验证

```
node tools/_runtests2.cjs          # 82 项单测：8 个文件逐个 spawn（一起跑会超 shell 超时，polish 一个文件就要 3 分钟）
node tests/validate-geometry.cjs   # 100 关 × 4 档屏幕的几何：棋盘、木框、物品栏、图标、名字、分区、固定件
node tests/validate-levels.cjs
node tools/layout-sweep.cjs        # 13 档屏幕的版式对照表（改版式前后各跑一次）
node tools/overflow.cjs            # 12 个真实视口宽度：横向溢出与棋盘格大小
node tools/_verify-mobile.cjs      # 真浏览器 35 项：手机版式、按钮两排、真交互放置、320 窄屏、桌面矮窗口
node tools/_verify-hint.cjs        # 真浏览器：救援三按钮
node tools/_verify-privacy.cjs     # 真浏览器：隐私弹窗五场景
node tools/music-check.cjs
node tools/nearfull.cjs --json=tools/nearfull-table.json   # 重新生成近满盘配置，报告见 tools/nearfull-report.txt
node tools/apply-nearfull.cjs                             # 合并进 core.js 的 HARDEN
node tools/nearfull-verify.cjs                            # 按 K.levels 复验成品（不是快照）
node android/tools/verify-apk.cjs
```

**82 项测试全部通过**（约 4 分钟，其中 `polish.test.cjs` 一个文件就要 3 分钟——「每关都摆得出最好那一档」要爬 100 关的山）；几何问题 0（100 关 × 4 档屏幕），单关最多 18 件；100/100 关卡可解；24/24 条旧物关系都能真正摆出来，18 件旧物全部出场；背景音乐体检全过（两个循环 246 个音符、每小节 8 个和弦波、循环点与开头逐音符一致）。近满盘那 82 关另有独立复验：`node tools/nearfull-verify.cjs` 会把「一点提示」依赖的性质逐关验一遍——可解、可完成、空位 ≤ 1、**沿着一个解逐件固定后每一步仍可解**，最慢一步 25235 节点（提示预算 70000）。APK 校验全绿：18 个条目、7 个游戏文件与源文件逐字节一致、条目名全部正斜杠、v1+v2+v3 签名有效、无测试或文档被打进包。详细记录见 `TEST_REPORT.md`。`?test=1` 为测试提供调试访问；正常入口不暴露调试接口。

## 当前限制

- 这是浏览器可玩的本地版本，不是已上线的 TapTap 游戏。
- **APK 尚未在真机上启动过。** 这台机器没有安卓设备也没有模拟器，能验证的是包结构完整、游戏文件与源文件逐字节一致、签名有效、清单字段正确；WebView 里的实际渲染与触摸手感待真机确认。首次安装重点看白屏、BGM 起播、拖动会不会被页面滚动抢走、杀进程后存档还在不在。
- APK 与小游戏两个渠道若同时发布，官方要求接入 TapTap 登录并打通两边存档，目前还没做；当前 APK 的存档只在手机本机。
- 未接入 TapTap 登录、平台存档、支付、平台分享或其他发布必接能力；没有真实收费。
- **激励视频已接好适配层，但广告位还没申请。** `ads.js` 里的 `AD_UNIT_ID` 是空字符串，按「未接入」处理——提示与消除都会直接放行，玩家不会因为拿不到广告卡住，按钮文案也跟着不写「看广告」（见上方玩法说明）。要到 TapTap 开发者后台（或 Dirichlet 媒体管理平台）建好「激励视频」广告位、把 ID 填进去，才会真的走广告；`STRICT` 开关决定拿不到广告时给不给奖励。顺带一提：文案变短之后，桌面工具栏从「七个按钮折成两排」回到了设计稿的**一排**（`tools/_verify-mobile.cjs` 会顺带量到 `.tools` 的高度）。
- **TapTap「H5 快速上架」（WebView 承载）里有没有 `tap` 全局对象，尚未确证。** 官方文档目前只覆盖 JS VM 小游戏，所以 `ads.js` 按「拿不到就降级」写：网页版、WebView 版都能正常玩，只是没有广告变现。真机接入前需要在目标渠道里实测一次 `typeof tap`。
- 数据仅保存在当前浏览器同一来源的本机存储中，不能跨设备同步。清理站点数据会删除存档。
- 本地事件记录只用于调试，最多200条，不上传网络；不能据此声称已有留存或变现成绩。
- 100 关用于看内容消耗与玩法变化，仍不能验证 D7／D30。整齐度的口径和上限是离线实测出来的，换关卡后需重跑 `tools/ceiling.cjs` 与 `tools/gradient.cjs`。
- 整齐度有一处已知短板：93 关非教学关里有 39 关，**朴素摆法**（求解器的左上贪心解）本身就有 95 分以上，认真摆也只好 1–4 分。这些关的分数几乎不动，玩家感受不到「再整一整」的空间。这是评分口径问题（各关可达上限不同），不是关卡问题，且第二辑比第一辑更轻（梯度中位 9 对 6）。改法见 `NEXT_VERSION_PROPOSAL.md` 的「按关卡自身上限归一化」。
- 作品与文案为本次制作；物品当前使用程序插画，正式版可替换为统一规格的手绘素材。
- 背景音乐是现场合成的，音色由浏览器实现决定，不同设备听感会有差异；只有开关、没有音量滑杆。**首次进入要在页面上点一下才会响**——这是浏览器的自动播放策略，不是故障。iOS 静音开关拨到静音时，Web Audio 也会没声。
