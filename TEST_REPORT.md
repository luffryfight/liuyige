# 留一格 验证记录

版本：1.0.3；日期：2026-09-14。验证对象为本地 HTML + JavaScript + Canvas 版本，未提交 TapTap。

## 测试执行

```
node tools/_runtests2.cjs        # 逐文件 spawn，结果写 tools/_all-tests.txt（放后台跑最稳）
```
等价于逐个文件跑（一次跑全部会超过 shell 超时；`polish.test.cjs` 单文件约 200 秒是正常的）：

```
node --test tests/core.test.cjs tests/polish.test.cjs tests/difficulty.test.cjs \
  tests/input.test.cjs tests/render.test.cjs tests/layout.test.cjs \
  tests/privacy.test.cjs tests/hint.test.cjs tests/gesture.test.cjs \
  tests/freeze.test.cjs tests/ads.test.cjs tests/rotate.test.cjs
```

结果：**114 项全部通过，0 失败**（core 12 / polish 15 / difficulty 8 / input 18 / render 1 / layout 9 / privacy 11 / hint 9 / gesture 7 / freeze 3 / ads 14 / rotate 7）。`core.js`、`game.js`、`ads.js` 语法检查通过。

另有几个独立校验脚本（不是 `node:test` 用例，需单独执行）：

| 脚本 | 结果 |
|---|---|
| `node tools/overflow.cjs` | 窄视口体检：12 个宽度（320~1280）全部无横向溢出；窄屏棋子格 42.6~78.3 CSS px，宽屏原稿 37.7~40 CSS px（原稿的取舍是「一屏看全」，格子大小是它的代价） |
| `node tools/_verify-mobile.cjs` | 真浏览器手机视口验收 35 项：棋盘 / 箱子尺寸、按钮两排与可点尺寸、页面顺序、无横向溢出、点选放置、320 窄屏、**桌面矮窗口仍是原设计稿** |
| `node tools/layout-sweep.cjs` | 版式对照表：13 档屏幕的画布宽 / 棋盘格 / 箱子格 / 物品栏形态 / 画布高，并量出两套稿接缝处的跳变 |
| `node tests/validate-geometry.cjs` | 100 关 × 4 档屏幕的几何（不越界、不压线、图标与名字放得下、分区与固定件合法） |
| `node tools/_verify-hint.cjs` | 真浏览器救援三按钮验收 30 项（提示 / 点名元凶 / 代放 / 死局代放 / 消除 / 撤销 / 文案不谎称看广告） |
| `node tools/_verify-privacy.cjs` | 真浏览器隐私弹窗验收 23 项（同意 / 拒绝 / 重开 / 焦点 / 链接可达） |
| `node tools/nearfull-verify.cjs` | 关卡数据复验：按 `K.levels` 逐关核验（改过关卡必跑） |

## 界面完善：手机版式与操作区（本轮）

需求是「手机上玩的时候，收纳工作台会比较小，操作不显眼，同时旧箱子也是，物品显示大小比较小，每一排可以数量少一点，多一排」。

根因：画布是一张按 CSS 宽度等比缩放的逻辑画布，原先只有一套 640×640 的稿子，棋盘居中只占 270 左右、两侧各空掉近 185，放到手机上缩放比只剩 0.57。改成按视口宽度分两套版式，`game.js` 的 `computeLayout()` 一次算清全部坐标，`render()` 不再有写死的 367 / 380 / 400。

| 检查 | 改版前（390 屏宽） | 改版后 |
|---|---|---|
| 画布逻辑尺寸 | 640×640 | 400×（内容决定的高度） |
| 棋盘格 | 25.7 CSS px | **51.9 CSS px** |
| 棋盘占画布宽 | 42% | **85%** |
| 箱子每格 | 51.5 CSS px | **78.5 CSS px** |
| 箱子排布（18 件） | 6 列 × 3 行 | **4 列 × 5 行** |
| 操作按钮 | 7 个挤在一排，44px 高 | 上排 3 个救援 117×64、下排 4 个基础 86×48 |
| 页面顺序 | 先委托信与委托簿 | 游戏区提到最前 |

### 断点：只认视口宽度

两套稿必须有一条分界线。第一版用的是「视口 ≤650 **或** 画布本身 <480」，第二条件是想照顾横屏手机，结果把桌面坑了：宽视口下画布宽度取的是 `100dvh - 350`，1280×800 的窗口算出来只有 448 —— 正好落进 <480，桌面就被判成手机，换成一张比窗口还高的长画布（棋盘格从 38.7 涨到 66 是好事，但物品栏掉到屏幕外，拖一件东西要来回滚）。

改成**只看视口宽度 ≤650**，和 `style.css` 的媒体查询共用同一条线。桌面窗口矮一点只当是「小一号的桌面」：原稿的卖点是一屏看全，`tools/layout-sweep.cjs` 里 13 档屏幕的所有宽屏行都是「画布高 − 窗口 = −350」（有余量，不用滚），而改回手机稿的那些行会要滚。

`node tools/layout-sweep.cjs` 实测（`drawer-78`，6×6 盘面 18 件）：

| 视口 | 画布 | 版式 | 棋盘格 | 箱子格 | 物品栏 | 画布高 |
|---|---|---|---|---|---|---|
| 320×568 | 296 | 手机 | 42.2 | 63.8 | 4 列×5 行 | 596 |
| 390×844 | 366 | 手机 | 52.2 | 78.9 | 4 列×5 行 | 737 |
| 540×960 | 516 | 手机 | 73.5 | 111.3 | 4 列×5 行 | 1038 |
| 650×900 | 626 | 手机 | 78.3 | 107.9 | 5 列×4 行 | 981 |
| 660×900 | 550 | 原稿 | 38.7 | 77.3 | 6 列×3 行 | 550 |
| 1280×800 | 448 | 原稿 | 31.6 | 63.3 | 6 列×3 行 | 448 |
| 1440×1200 | 850 | 原稿 | 59.8 | 119.5 | 6 列×3 行 | 850 |

手机段（42.2 → 78.3）随画布单调增长，没有回头路；接缝在视口 650，格子从 78.3 掉到 38.7（−49%），但这一档整页的排布也在同时变（按钮分两排、游戏区提到最前、委托簿换位），所以玩家看到的是「整页换模式」，不是画布单独抽风。手机段之所以不按 1:1 铺开，是因为 650 视口的画布能到 626：再按逻辑宽 400 渲染会放大到 1.5 倍，格子没大多少、画布却高得离谱，所以逻辑宽跟着画布走，把缩放封在 1.35 倍以内。

### 断言

| 检查 | 结果 |
|---|---|
| 100 关手机版式几何（不越界、不压线、格子够大） | 100 / 100 通过 |
| 100 关宽屏版式与改版前逐项一致（逻辑尺寸、棋盘格、分隔线、箱子列数） | 100 / 100 通过 |
| 手机版式下「点物品 → 点格子」放置 | 通过 |
| 手机版式下拖进箱子区域放回桌面 | 通过 |
| 320 / 390 宽无横向溢出，按钮均不低于 44px | 通过 |
| 桌面 1280×900 仍是原设计稿（640×640、分隔线 380、18 件 6 列） | 通过 |
| 宽视口 + 画布被压窄（1280×800 窗口，画布 448）仍是原设计稿，不换成要滚动的长画布 | 通过 |
| 画布宽度不变、只把视口换窄到 390 → 切手机版式（判据是视口，不是画布） | 通过 |
| 100 关 × 4 档屏幕（桌面 1440×900 / 1280×800、手机 390、小屏 320）的几何 | 0 问题 |
| 广告位未配置时，三个救援按钮不写「看广告」（与游戏内隐私政策「没有接入任何广告 SDK」对得上） | 通过 |

## 本轮：手机端翻转一件已经放好的旧物

玩家报：「在手机端，翻转一个已经摆放上去的物品不是很好操作」。

查下来是两个叠加的原因，跟「按钮小」其实没关系：

1. **入口太远。** 翻转只有一条路——先在棋盘上点中那件旧物，再滚到画布下方的工具栏按「旋转」。手机版式一张画布比一屏还高（`drawer-83` 上文档 1759px、视口 700px），选中的旧物多半在视口外的那一半，来回滚一趟很别扭。
2. **按了没反应。** 原地转不开时老版本只回一句「原地转不开」，不消耗步数、画面一个像素都不变。盘面挤到后面，长条原地旋转十有八九会压到旁边那件，于是玩家看到的就是「点了、什么都没发生」。

顺带排除了一条可能性：这不是命中区太小的问题——真机量到翻转键 54×54 CSS px，远在 44px 门槛之上；棋盘格在手机版式下是 42.6~78.3 CSS px，也够点。

### 修法：一个就近挪位 + 两条够得着的入口

- **转不开就近挪位。** `rotateSpots(id,rot)` 枚举所有合法落点，按「转完之后和原来的格子重叠最多 → 挪得最少 → 靠上 → 靠左」排序取第一个。原地能转时它的结果就是原地（重叠必然是满格），所以老的「原地转」路径改成先直接判 `canPlace`，行为与改版前逐字一致——**转成功时连状态栏里的旧物回忆都不覆盖**，挪过位才补一句「原地转不开，就近挪了 N 格」。真无处可去才照实说，且不动盘面（`event('rotate',{blocked:true})`）。
- **连着点两下已放好的旧物 = 就地翻转。** 单点仍是选中；双击要三条同时成立——同一件、间隔 ≤320ms、**而且它此刻就是选中的那件**。最后这条是安全绳：中间只要点过别处（放下、取消、选了另一件），选中就变，绝不会误判。只对「已经放好的」生效，托盘里那件仍只选中。
- **右下角一个 54px 的翻转键**（`#rotate-fab`），只在手机断点、且选中了可翻转的旧物时才出现，宽屏由 CSS 一律藏掉。它和工具栏的「旋转」按钮走同一个 `rotate()`。做成 `position:fixed` 而不是画在画布上，是因为画在里面会压住棋盘格子——而「点格子放下」正是选中状态下最频繁的动作。

两个入口共用 `canTurn(id)`（不是委托人固定的、也不是本关规定「只能按原方向放」的），所以「不能转的旧物不出现翻转键」和「工具栏按钮禁用」永远同步。

### 测试与反向验证

新增 `tests/rotate.test.cjs` 7 项 + `tools/_verify-rotate.cjs` 18 项（真 Chrome、真触摸、真按钮点击）：

| 守住什么 | 怎么守 |
|---|---|
| 原地转得开时位置不动、回忆文案不被覆盖 | 摆好 `drawer-1` 的 tape 再转，比 `placed` 与状态栏原文 |
| 转不开时按「重叠最多、挪得最少」落位 | `drawer-1` 的 tape 横躺在最后一行转不开，断言落到最靠左的那处重叠位 |
| 真无处可去时不动盘面 | 用求解器把 `drawer-9` 铺成满盘，长条转过来哪都放不下 → 断言 `placed`、`moves` 不变且文案说明原因 |
| 双击转一次、第三下只选中 | 连点三下 |
| 两下之间动过别处就不算双击 | 点中 → 放到别处 → 再点，方向不许变 |
| 翻转键的显隐与尺寸 | 真 Chrome：没选中 `display:none`、选中后 54×54 且贴右下角、宽屏一律 `none` |
| 真手指按翻转键真的转 | CDP 派发真 touchstart/touchend |

反向验证（把修复掐掉看会不会红）：就近挪位退回「直接拒绝」→ 那条红；去掉双击判定 → 双击 2 条红；翻转键不再按选中状态显隐 → 手机端 1 条红；去掉宽屏藏匿规则 → 宽屏 1 条红。四次都如期变红，回滚后复跑全绿。

### 本轮没验的部分

- **双击的 320ms 窗口没在真手指上量过。** CDP 合成的触摸时序比真人规整；真机上误判／漏判只需调 `DOUBLE_MS`。
- **翻转键和工具栏有一小段位移会抢右下角。** 滚到工具栏正好经过视口右下角时，它会压住「重新整理」的右半边；滚到底或回到画布都不冲突（真机量过：滚到底后两者完全不重叠）。当前刻意不做滚动期隐藏，理由与代价写在 README 的「当前限制」里。

## 本轮：画布手势、摆放卡顿、依次解锁与广告接入口径

玩家报了三条，外加一条完善要求：

1. 手机版式下（一张画布比一屏还高时）**在旧物箱和收纳台上按住上下滑，页面滚不动**；
2. **大师委托第一关放上一件旧物就卡死**，连音效都停；
3. **关卡要依次解锁**——默认只开第一关，通关一关才开下一关；
4. 点「提示／帮我放一件／消除」要看 TapTap 激励视频，看完才发奖励，接完再上传包体。

这一轮**没有发版、没有打包**：广告位还没开通（`check_ads_status` 返回 0），按既有决定「等广告开通后一起传」，避免同一个版本号传两次。改动都已在工作区里，随下一版一次性发出去。

### 卡顿：根因是同步求解器，不是渲染

`updateUI()` 每次放置／选中都会调 `clearCandidate()`，而它为了决定「消除」按钮灰不灰，会在每个候选上跑一次完整求解。**还没放下的候选删掉之后盘面完全相同**，等于同一道题解十几遍。

用探针（`tools/_probe-freeze.cjs`，已删）在 `drawer-44` 空盘面的 8 个合法位置上量到：

| 位置 | 求解次数 | 耗时 |
|---|---|---|
| 5 个位置 | 32 次 | 15.2 ~ 16.7 秒 |
| 3 个位置 | 1 次 | 0.03 ~ 0.06 秒 |

单次满额求解 = 51534 节点 ≈ 1 秒，所以「放一件卡十几秒」的量级对得上。

三层修补：

- `updateUI()` 改用**纯规则筛选**的 `clearPool()`，不判可解性（按钮灰不灰本来也不需要求解器）；
- `clearCandidate()` 按「删掉该件之后的局面键」**去重**，同一道题不再重复解；
- `solve/solveAll` 增加可选 `deadline` 参数（每 64 节点看一次表，超时返回 `status:'timeout'` + `expired`），按钮路径统一用 `SOLVE_MS=1200` 预算，超时就照实告诉玩家「这一局绕得有点久」。

离线校验与 `polish.test.cjs` 不传 `deadline`，行为与改版前逐字一致（100 关全解仍通过）。

### 手势：三种意图分开判

画布挂着 `touch-action:none`（不挂的话拖旧物会被浏览器抢走），代价是手指按在画布上怎么划页面都不动。现在按下时**推迟定性**：

| 手指动作 | 判定 | 结果 |
|---|---|---|
| 按住 ≥130ms（`HOLD_MS`）再移动 | 搬旧物 | 页面不动 |
| 按住后直接划过（位移 ≥ `SLOP=8`） | 滚页面 | 自己 `window.scrollBy`，松手带惯性（`FLING_DECAY=.94`） |
| 按下即抬起 | 轻点 | 选中／把已选旧物放进这一格 |
| 空白处（没压到旧物） | 滚页面 | 那里本来就没东西可搬 |

鼠标（`pointerType === 'mouse'`）走原路径，桌面行为逐字不变。触摸收尾与按下对称释放 pointer capture——原代码在 locked 分支确实漏了 release，会残留。

### 依次解锁

`unlockedLimit()` = 存档里第一个没有 `completed` 的关卡下标，**只用既有字段**，老存档不需要迁移。锁着的关卡写「锁」、用 `aria-disabled` 而不是 `disabled`（真禁用的按钮收不到 click，玩家只会觉得「按了没反应」），点了给一句「第 0X 关「…」整理完，这一份委托才会打开」。`?unlock=all` / `GameDebug.unlock.all(true)` 是会话级自测开关，**刻意不写进存档**。

深链（`?level=N`）不受解锁钳制，因为自测与截图脚本要靠它直接进任意一关；普通进入（用存档里的「上次玩到哪」）如果落在锁着的关卡上，会被带回第一个未完成的关卡并说明原因。

### 广告：先算方案，再播广告

三个入口都拆成「算 / 落」两步（`planHint/applyHint`、`planAuto/applyAuto`、`clearCandidate/doClear`），**先算出结论再播广告**——算不出来就不播，不能让玩家看完才被告知「这一步没法替你做」。`tests/ads.test.cjs` 用假 SDK 验的：

| 场景 | 期望 | 结果 |
|---|---|---|
| 每关第一次提示 | 免费，不碰广告 | ✓ |
| 第二次提示 → 看完 | 发奖励，且 `adUnitId` / `spaceId` 两个键名都带上 | ✓ |
| 中途关掉（`isEnded:false`） | 不发奖励，文案与 `ads.js` 的 `REASON` **逐字一致** | ✓ |
| 宿主只回调 `onReward` | 也算看完，不吞奖励 | ✓ |
| `show()` 先失败 | `load()` **一次**再试，成功照发；重试也失败则不发 | ✓ |
| 只在重试一次 | `shown` 恰好 2 次，不进死循环 | ✓ |
| 网页版没有 `tap`／广告位为空 | 直接放行并说明，按钮文案不写「看广告」 | ✓ |
| 额度用完 | **不再播广告** | ✓ |
| `STRICT=true` | 拿不到广告就不给（开关不是摆设） | ✓ |

顺手修掉的两个真问题：`withAd` 原来完全不看 `STRICT`；免费放行时写进状态栏的那句说明立刻会被动作自己的文案盖掉，是死代码，已删掉并把说明归到玩法说明／政策里（`KeepsakeAds.status()` 仍导出，供文档引用）。

**合规闸门：** `tools/set-ad-unit.cjs` 在写入广告位 ID 之前会检查 `index.html` 隐私弹窗／`docs/privacy.html`／`docs/privacy.md` 里那 5 行「没有接入任何广告 SDK」。ID 一非空，游戏就真的会产生网络请求与第三方数据处理，那 5 行就成了假话，而 TapTap 的口径是「政策须真实有效、与 App 内协议一致」——所以**文案没跟上就拒绝写入**。当前 ID 为空，那 5 行此刻是真话，闸门报告「当前一致」。

### 测试与反向验证

单测 **82 → 107 项**（新增 `gesture` 7、`freeze` 3、`ads` 14，全部通过）；真浏览器新增 `tools/_verify-scroll.cjs` **36 项**，用 CDP 派发**真触摸事件**（不是页面里造 PointerEvent）。

三组新测试都做了反向验证——把修复掐掉，看测试是不是真的会红：

| 掐掉什么 | 结果 |
|---|---|
| 不再 `scrollBy`（假 DOM） | 滚动 2 条红 |
| `updateUI` 换回跑求解器 | 摆放 2 条红（实测一次摆放 4 次求解、连摆带取 14 次） |
| 不再 `scrollBy`（真 Chrome 真触摸） | 滚动 2 条红 |

反向验证还揪出一处**虚假通过**：真浏览器脚本里「划过旧物箱」原本比的是 `scrollY` 绝对值，而脚本为了把旧物箱滚进视口已经先把页面滚下去了（319px），于是那一划哪怕完全没生效也照样「通过」——改成比增量之后才真正守得住。同时给所有手势用例加了前置断言「手指落点必须在视口内」：落在视口外的话，滚页面的是**浏览器原生滚动**，测出来的根本不是我们要守的东西。

本轮回归全绿：

| 验收 | 结果 |
|---|---|
| 单测（11 个文件） | 107 / 107 |
| `tests/validate-geometry.cjs` | 100 关 × 4 档屏幕，几何问题 0 |
| `tools/layout-sweep.cjs` | 13 档屏幕全正常，两套稿接缝无异常 |
| `tools/overflow.cjs` | 12 个真实视口，横向溢出 0 |
| `tools/_verify-mobile.cjs` | 35 / 35 |
| `tools/_verify-scroll.cjs`（新） | 36 / 36 |
| `tools/_verify-hint.cjs` | 30 / 30 |
| `tools/_verify-privacy.cjs` | 23 / 23 |
| `tools/nearfull-verify.cjs` | 82 关全通过（可解、空位 ≤ 1、逐件固定后仍可解） |

### 本轮没验的部分

- **真实激励视频一次都没跑过**：广告位未开通，`show()` 的真实行为、`isEnded` 回调和真机播放都还是纸面逻辑。
- **`window.tap` 在 TapTap「H5 快速上架」（WebView 承载）里是否存在，仍未确证。** 官方文档目前只覆盖 JS VM 小游戏；接完广告要在目标渠道里实测一次 `typeof tap`。
- **真机触摸手感未验**：`_verify-scroll.cjs` 跑的是 CDP 合成的触摸事件，与真手指在惯性、采样率上仍有差异。130ms 的按住阈值需要真机上试。
- **APK 未在真机上启动过**（沿用上一轮的结论）。
- 政策文案这次**没有改**，因为广告确实还没接入，改了就是假话；改写动作被闸门绑在写入广告位 ID 的那一步。

## 本轮（1.0.3）：近满盘难度与激励视频

需求是「从留物取舍开始后面所有关卡难度提升、增加要摆放的物品位置、通关时空位最多一个」和「10–20 件，点击提示看广告，或者看广告消除」。

### 关卡配置：82 关逐关过求解器

| 检查 | 结果 |
|---|---|
| 覆盖范围 | `drawer-16` ~ `drawer-100`，82 关（`drawer-15` 是该章教学关，保持轻量） |
| 生成成功 | 82 / 82，0 失败 |
| 通关空位 | 0 格 78 关 / 1 格 4 关 / **超过 1 格 0 关** |
| 物品栏件数 | 10 ~ 18 件 |
| 棋盘尺寸 | 缩小 0 关 / 持平 36 关 / 放大 46 关（生成器写死了「只准放大」） |
| 取舍关挑法数 | 每关至少 6 种能通关的挑法，实测多数 100 种以上（最多 680 种） |
| 求解规模 | 全量生成共 59495 次求解调用，约 12 分钟 |

成品另做一次独立复验（`tools/nearfull-verify.cjs`）。**这一步是必须的**：生成器读的是 `K.RAW` 快照，而运行时还要再叠 HARDEN 与关系补丁，两边差一件物品结论就不成立——初版就踩了这个坑，`drawer-21/22/31` 在快照里「验过」，装上后变成无解（多出一把旧木尺，格数 49 > 可用 45）。

| 复验项（按 `K.levels`，即玩家真正打开的数据） | 结果 |
|---|---|
| 可解 / 可完成 / 空位 ≤ 1 | 82 / 82 |
| 逐件固定后仍可解（「一点提示」要在摆了一半的盘面上给下一步） | 82 / 82 |
| 最慢一步的节点数 | 25235（`drawer-27`），提示预算 70000，余量充足 |
| 节点超 6 万预算的关卡 | 0 |

### 激励视频

`ads.js` 是纯静态页面与 TapTap 客户端双环境兼容的适配层，**没有真实广告位可以联调**，所以验的是「拿不到广告时的行为」，而不是广告本身：

| 检查 | 结果 |
|---|---|
| 未配置广告位（`AD_UNIT_ID` 为空） | `supported()` 为假 → 直接放行，提示与消除照常可用，状态栏说明原因 |
| 「消除一件」的可解性预检 | 在播广告**之前**先算好消哪件，并用求解器验过「消掉之后仍可解」；算不出可消件时直接说明，不让广告白看 |
| 失败原因 | `ended / skipped / unavailable / load-failed / timeout / busy` 逐个给不同文案，不静默失败 |
| 发奖励的判定 | 只在 `onClose(res => res.isEnded)` 为真时发；`show()` 返回不代表看完 |

**没验的部分要说清楚：** 真实激励视频的加载、播放、`isEnded` 回调都没有在真机/真环境下跑过；TapTap「H5 快速上架」（WebView 承载）里有没有 `tap` 全局也还没确证。官方文档目前只覆盖 JS VM 小游戏。

### 打包产物

| 产物 | 结果 |
|---|---|
| `dist/liuyige.zip`（TapTap H5） | 9 个条目、压缩后 55.7 KB、第一级文件夹唯一、入口在第二层、逐文件与源文件一致 |
| `dist/liuyige.apk` | 18 个条目、89.3 KB、7 个游戏文件与源文件逐字节一致、条目名全部正斜杠、v1+v2+v3 签名有效、无测试或文档被打进包 |

## 规则与几何回归

| 检查 | 结果 |
|---|---|
| 100 个关卡可解；允许多个合法答案 | 通过 |
| 旋转保持占格数量；四次旋转复原 | 通过 |
| 越界、重叠、未知物品和损坏布局拒绝 | 通过 |
| 移动物品不会与自身旧位置碰撞 | 通过 |
| 耳机凹槽可容纳物品，未用外接矩形错误拦截 | 通过 |
| 提示保留已有摆放；区分无解与搜索上限 | 通过 |
| 隔板阻挡占格，旋转后仍成立 | 通过 |
| 分区限制作用于每一格，旋转后仍成立 | 通过 |
| 取舍关必须满足配额且带齐必留件 | 通过 |
| 每份取舍委托都至少有两人份不同的可行选择 | 通过 |
| 恰好放满的委托：物品格数正好等于抽屉空格数 | 通过 |
| 固定件位置合法、不可移动、求解器必须照着放 | 通过 |
| 锁转物品只有原方向合法 | 通过 |
| 物品栏随件数分列，最多不越过画布底边；手机上每排少放一件、多排一行 | 通过 |

## 输入与存档回归

| 检查 | 结果 |
|---|---|
| 右键直接撤回命中物品，保留其他物品；撤销和存档正确 | 通过 |
| 右键空白及桌面不误删，触摸 contextmenu 不执行撤回 | 通过 |
| 单击、非法拖放、成功重放后移动鼠标，不再产生跟随预览 | 通过 |
| 丢失抬起事件后，检测鼠标按键已松开并取消拖动 | 通过 |
| 指针取消、捕获丢失、窗口失焦、页面隐藏均清理拖动 | 通过 |
| 第二根指针不接管当前拖动 | 通过 |
| 旋转按钮、R、方向键、Enter、放回桌面和撤销可用 | 通过 |
| Escape 取消并释放指针捕获 | 通过 |
| 刷新恢复当前关卡与物品；非法关卡编号回退首关 | 通过 |
| 固定件在真实交互里不能拖、不能转、不能收回、也不能被顶到别的位置 | 通过 |
| 取舍流程拒绝“只凑数量”，允许换件，撤销后能恢复完成状态 | 通过 |
| 100 份委托都能走真实游戏流程通关（含按规则给出的提示） | 通过 |
| 每个章节标签都能打开自己的委托，棋盘缩放后不与物品栏重叠 | 通过 |

输入测试在 Node vm 中执行真实游戏事件处理函数，DOM、存储和事件派发由测试替身提供；不包含画面渲染，也不代表浏览器或手机原生事件均已覆盖。

## 本轮新增内容的验证

| 检查 | 结果 |
|---|---|
| 新手章节由七种玩法各一关组成，且排在全部委托最前面 | 通过 |
| 新手章节保持教学难度，不被难度增强表改写 | 通过 |
| 难度增强表逐条落到关卡上，没有“写进表却漏生效”的配置 | 通过 |
| 18 种旧物每一种都至少在一份委托里出现过 | 通过 |
| 24 段关系的两件物品至少共存于一份委托，否则永远解不开 | 通过 |
| 四种关系判定各自成立，且不会误判（相邻但只碰到一个角不算） | 通过 |
| 整齐度随摆放实时计算，空抽屉为零分 | 通过 |
| 同一份委托的两种摆法会得到不同分数，分数不是摆设 | 通过 |
| 留白看的是空位去哪儿了：推到边上得分高，堵在中间扣分 | 通过 |
| 把相关的两件摆到一起会解锁图鉴，并写进本机存档 | 通过 |
| 同一段记忆不会重复计数，移走也不再回收 | 通过 |
| 图鉴面板渲染全部 24 段，且在打开时才构建 | 通过 |
| 章节标签会标记当前章节（按 DOM 属性读） | 通过 |
| 解锁的新记忆会出现在完成提示里 | 通过 |
| 每一关都摆得出最好那一档，没有“怎么摆都听不到好话”的关卡 | 通过 |
| 委托簿每个章节的序号都从 01 起、不跳号（章内顺序，不是全书顺序） | 通过 |
| 声音开关同时驱动 BGM、按钮文案与存档偏好，且没有 AudioContext 时不抛异常 | 通过 |

## 离线校验（tools/）

| 校验 | 方法 | 结果 |
|---|---|---|
| 多解 | `tools/multisol.cjs` | 100 关多解 / 0 唯一解 / 0 不可解 |
| 分数上限 | `tools/ceiling.cjs`，多起点爬山 | 最低 84，P25 95，中位 98，P75 100，最高 100；评语线 82 |
| 整齐度梯度 | `tools/gradient.cjs` | 朴素解 → 最好解的提升：中位 7，最大 26；39/93 关低于 5（见“已知短板”） |
| 分数跨度 | `tools/spread.cjs` | 每关跨度中位 11，最大 32 |
| 维度分布 | `tools/dims.cjs` | 三档维度在每关都拿得到高分 |
| 关系可达性 | `tools/relations-check.cjs` | 24 / 24 可达（56 次求解器调用） |
| 物品使用 | `tools/calibrate.cjs` | 18 / 18 至少出现一次 |
| 关卡结构 | `tools/baseline.cjs` | 100 关 / 8 章节；件数 4–10，盘面最大 8×6 |
| 背景音乐 | `tools/music-check.cjs` | 两个循环排出 246 个振荡器；每小节恰好 8 个和弦波（4 音 × 2 失谐）；低音每小节两下；第 9 / 17 小节与第 1 小节逐音符一致；音域 73.4–3537.6 Hz；关闭后不再产生新节点且全部已安排停止 |

### 背景音乐是怎么验的

单元测试跑在 Node 里，测试替身没有 AudioContext，所以「声音开关」那条只能证明**逻辑**对，证明不了音乐本身。于是分两头验：

- **离线（`tools/music-check.cjs`）**：造一个只记账不发声的假 AudioContext，把 `music.js` 原样加载，用假时钟推着前瞻调度器走完两个循环，再核对排出来的音符——每小节是不是恰好 4 个和弦音、低音是不是每小节两下、循环点是不是回到第 1 小节、音高是不是有限值且在可用音域内、关闭后有没有残留没安排停止的振荡器。另外假 `AudioParam.exponentialRampToValueAtTime` 会照着真实 Web Audio 的约束拒绝 **≤ 0** 的目标值，因为「指数斜坡到 0」正是最容易写出浏览器直接抛异常的坑。
- **真实浏览器（`tools/music-probe.html`）**：假的 context 验不了真实现。探针在无头 Chrome 里加载 `music.js` 并真的 `set(true)`，确认真实的 ConvolverNode（脉冲响应）、DynamicsCompressor 参数与全部包络斜坡都建得起来。实测结果：`supported=true`、`set(true)=true`、`playing=true`、**抛出异常 none**、300ms 后仍在播、`set(false)` 后 `playing=false`。

音乐好不好听得靠人耳朵，上面两条只保证它**排得对、建得起来、关得掉**。

### 已知短板：整齐度在部分关卡里几乎没有梯度

`tools/gradient.cjs` 比较「朴素摆法」（求解器的左上贪心解）和「认真摆过的最好分数」。93 关非教学关里有 **39 关的差距小于 5 分**，其中 10 关干脆是 0 分——随手摆 97，认真摆还是 97。

原因是评分口径按绝对分给评语，而各关的可达上限本来就不同：这 39 关的抽屉格少、形状规整，随手塞进去就已经「对齐、留白、居中」都接近满分。这是评分设计问题，不是关卡问题，而且**第二辑比第一辑更轻**（梯度中位 9 对 6），扩关没有让它变差。改法记录在 `NEXT_VERSION_PROPOSAL.md`。

## 本轮修掉的三个真实缺陷

**1. 难度增强生成器读的是「已经增强过」的关卡，导致它长期无法重跑。**
`tools/harden.cjs` 靠 `tier(seq).count - items.length` 算这一关该加几件。但 `core.js` 在装载时就调用 `applyHarden()`，所以生成器读到的 `items.length` 已经是加完之后的数字，差值恒为负——实测 100 关里只有 56 关产出条目，其余 44 关被整片 `continue` 跳过，而且部分老关卡的条目被算成了别的值。这解释了为什么这个生成器写完表之后就再没人跑过它。

修法是两份改动：`core.js` 导出一份 `K.RAW`（`applyHarden()` 之前的快照），生成器改读 `K.RAW`；同时把 `l.seq`（定义顺序）写进关卡，生成器按 `seq` 而不是 `K.levels` 的展示索引取档位——章节重排会把新手章节提到最前面，跟着展示顺序取档会让同一关拿到不同的目标件数。改完后重跑：**96 关全部产出、0 失败，且第一辑 46 条条目与旧表逐字节一致**，证明解耦没有改动已验收的内容。

**2. 「锁转」被挂在了取舍关，规则成了看不到的空话。**
新增锁转示范时，我按索引挑到了几关大师委托——它们都是 `keepCount` 关，玩家可以不选那件锁转旧物，于是「不能转着放」这条规则永远显示不出来，测试也读不到它在解里。改成只挂在**不取舍**的关卡（隔板／分区／隔板分区）上，现在 3 关锁转全部能在解里看到，且方向确为 0。测试也补了一行防守：若以后又把锁转挂回取舍关，只要求「在场时方向为 0」，不会因为玩家没选它就误报。

**3. 委托簿的序号用的是全书序号，按章节切过去像漏了关卡。**
列表按钮上的号码原本写的是 `K.levels` 里的全局下标。委托簿是按章节过滤显示的，于是切到「隔板抽屉」（共 13 关）看到的是 **13, 14, 15, 22, 23, 24, 52, 59, 66, 73, 80, 87, 94** —— 中间全是被别的章节占掉的号，第一眼会以为关卡缺失。改成**章内序号**：本章 13 关就编 01–13，八个章节各自从 01 起。全书进度仍由顶栏「X / 100 已整理」和信头「87 / 100」承担，两者不再互相冒充。截图见 `tools/shots/lv87-auto.png`（隔板抽屉读作 01–13）与 `lv45-auto.png`（大师委托读作 01–14）。

## 浏览器操作验证

用无头 Chrome 按固定视口（760 × 1400）打开 `index.html`，通过 `?level=<全书序号>` 定位委托、`?test=1&solve=1` 先摆上参考解再截图。截图脚本见 `tools/shots.cjs`，输出在 `tools/shots/`。注意 `?level=` 收的是**全书序号**（1–100），与委托簿里显示的章内序号不是一回事。

| 场景 | 观察结果 |
|---|---|
| 第 1 关开局 | 停在“新手章节”，8 个标签齐备；棋盘空，提示“放上第一件旧物就会开始计分：对齐、留白、居中各占一部分” |
| 第 14 关自动摆好 | 整齐度 96，分项 对齐 93 / 留白 100 / 居中 96；建议语随最弱项变化 |
| 第 33 关自动摆好 | 整齐度 98，分项 对齐 98 / 留白 100 / 居中 95，评语为“这一层已经挑不出毛病了” |
| 第 45 关（大师委托） | 规则卡完整显示“从 9 件中选留 4 件”“已固定 1 件”；固定件画虚线框与图钉，物品栏标“已固定”；整齐度 79 |
| 第 87 关（第二辑 · 恰好放满） | 规则卡显示“这一层要刚好放满，一格都不能空”；棋盘下方进度提示剩余格数 |
| 第 100 关（第二辑 · 收官） | 委托编号显示 `100 / 100`，章节写“基础收纳 · 第二辑 · 冬 · 最后一箱” |
| 图鉴面板 | 24 段全部渲染，已解锁标绿并显示文案，未解锁显示“未解锁 / 把这两件放在一起，看看会发生什么” |
| 章节标签高亮 | 切到“基础收纳”“大师委托”后对应标签正确高亮 |
| 旧物提示条 | 渲染为画布下方的独立卡片，不覆盖棋盘 |
| 背景音乐探针（`tools/music-probe.html`） | `supported=true`、`set(true)=true`、`playing=true`、**抛出异常 none**；真实 Convolver / 压缩器 / 指数斜坡都建得起来；`set(false)` 后 `playing=false` |
| 声音开关（游戏页） | 顶栏显示「声音：开」，点击后切到「声音：关」，`aria-pressed` 同步翻转 |
| 浏览器 error / warn 日志 | 检查时为空。`music.js` 加载时**不**创建 AudioContext（只在用户手势后才建），因此不会触发自动播放警告 |

## 尚待验证

- Android、iOS 实机触摸、长按、切后台、低端机性能和 WebView 差异。
- **背景音乐只验到「排得对、建得起来、关得掉」，没验过听感。** 32 秒循环会不会让人腻、音色在不同设备上差多少、iOS 静音开关与来电／蓝牙切换会怎么打断，都要真人真机试听。目前也没有音量滑杆，只有开关。
- PNG 在不同设备的下载、相册保存与平台分享。已验证生成和展示，未验证最终落盘。
- TapTap H5 平台能力、登录、云存档、激励广告、支付、审核及线上运行。
- 真实用户理解度、完成率、留存和商业指标；当前没有相应样本数据。整齐度是否真的让人愿意反复调整，需要真人试玩才能判断。
- 整齐度上限是用爬山法逼近的（不是穷举），属于下界估计；换关卡后必须重跑 `tools/ceiling.cjs` 与 `tools/gradient.cjs` 并同步评语线。评语线 82 与实测最低上限 84 只差 2 分，改任何评分权重前都要先重跑。

曾尝试独立 Playwright 脚本，但浏览器进程启动被运行环境拒绝，脚本没有执行游戏验收；以上浏览器结果来自无头 Chrome 截图。
