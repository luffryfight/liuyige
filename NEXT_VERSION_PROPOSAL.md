# 从操作原型到有变化的收纳游戏

**状态：上一版提案已交付并被超出，本文更新为下一轮的取舍。** 更新日期 2026-09-14，对应 1.0.4。

## 上一版提案的落地情况

上一版判断“6 关足以验证操作，但胜利条件始终是‘全部放入’，单纯堆到 50 关只会延长同一种体验”，计划保留 6 关、再加隔板／分区／取舍三个小组共 18 关。实际执行结果：

| 上一版计划 | 实际交付 |
|---|---|
| 6 → 18 关 | 50 关，8 个章节 |
| 隔板 / 分区 / 取舍 三组 | 六组规则组合（含隔板+分区、取舍+分区、大师委托） |
| 计划外 | **新手章节**（七种玩法各一关）、**整齐度评分**、**回忆图鉴** |
| 委托类型、规则说明、分组导航、存档兼容 | 全部落地；章节标签、规则卡、存档向后兼容 |

“单纯堆关数不能验证耐玩”这个判断在落地时被绕开了：新增的两套系统（整齐度、图鉴）都不是“更多关卡”，而是**同一关里的第二个目标**。摆完只是及格，摆得整齐才是终点；顺带摆对两件旧物的关系还能解锁一段图鉴。同一关因此有了三层收益。

## 本轮（0.3.0）：补齐 100 关

需求是“关卡数量创造到 100”。交付方式不是把同一批关卡复制一遍，而是先承认一个事实——

**难度的体量在第 50 关就已经到顶。** 物品栏最多放 10 件、盘面最大 9×8，再往上堆件数只会让「一点提示」的求解从秒回退化成超时。所以第二辑（第 51–100 关）的件数带**反而收窄**：前半段 8 件、后半段 9 件。多出来的难度来自三处：

| 手段 | 第一辑 | 第二辑 |
|---|---|---|
| 恰好放满（一格不能空） | 3 关 | 5 关 |
| 不能转着放 | 1 关 | 2 关 |
| 固定件（委托人事先放好） | 8 关 | 7 关 |
| 条件叠加（隔板+分区+取舍） | 有 | 全量铺开 |

第二辑另配了一条季节线索：同一章节里的 7 关按春→冬推进（回南天、落雨不停、蝉声、长昼、搬家季、起风、收店前、最后一箱），翻开委托簿能看出时间在走。这是让 50 份新委托不至于读起来像填空的最低成本做法。

**但必须说清楚：第 50 关之后的难度曲线是平的。** 如果要有第 101 关，它需要的是**新机制**，不是新关卡。

## 0.3.1 与 0.4.0：交付当天补的两处

- **0.3.1 委托簿序号改为章内编号。** 列表原本用的是全书下标，可委托簿是按章节过滤显示的，于是切到「隔板抽屉」（13 关）看到的是 13,14,15,22,23,24,52,59,66…——中间全是被别的章节占掉的号，第一眼像漏了关卡。改成章内序号（本章 13 关就编 01–13），全书进度交给顶栏「X / 100 已整理」和信头「87 / 100」，两者不再互相冒充。
- **0.4.0 背景音乐。** 用 Web Audio 现场合成一个 32 秒循环（慢起暖垫 + 音盒主旋律 + 心跳低音），**不挂音频文件**。选合成不选 mp3，是因为这个项目纯静态、零构建、双击就能玩：几 MB 的音频会让首屏和离线包都变笨重，而合成不占体积、循环处相位连续（没有接头爆音）、也不牵扯授权。首次进入需要点一下画面才响（浏览器自动播放策略），右上角「声音」一个开关同时管 BGM 和提示音，选择写进存档；切到别的标签页会自动淡出。
  - 顺带把提示音也接到同一个 `AudioContext` 上——移动端开两个 context 容易互相打断。
  - 新增两条验证：`tools/music-check.cjs`（假 context 离线核对音符排布）与 `tools/music-probe.html`（真实浏览器确认真实音频图建得起来）。**听感没验过**，那要人耳朵。

## 1.0.4：从「留物取舍」起要求摆到只剩一格 + 激励视频

需求是两条：「从留物取舍开始，后面所有关卡难度提升，增加要摆放的物品位置，通关时空位最多一个」；以及「10–20 件，增加难度，点击提示看广告，或者看广告消除」。

这一轮把上一节那句「第 50 关之后的难度曲线是平的」直接推翻了。**体量没到顶，是上一版的生成器到顶了。**

| | 0.4.1 | 1.0.4 |
|---|---|---|
| 适用关卡 | 第 51–100 关靠条件叠加 | **第 19–100 关（82 关）全部重配** |
| 件数 | 前半段 8 件、后半段 9 件 | 10 → 18 件线性抬升 |
| 通关空位 | 可以留白 | **最多 1 格**（取舍关也是） |
| 生成方式 | 手工定尺寸，跑求解器验一遍 | 先定件数，反解棋盘尺寸与追加件 |

关键判断有三个：

1. **「近满盘」对求解器反而更友好。** 当时以为加件数会把「一点提示」的求解拖成超时，实测相反：约束越强，MRV 剪枝越有效。18 件填满 9×8 只要 590 节点 / 18ms；82 关里最坏的一关也在 6 万节点预算内。**当初的「到顶」是把生成器的搜索成本当成了求解器的成本。**
2. **取舍关不能只算盘面。** 玩家人手只放 `keepCount` 件，「能凑满格数」的挑法本来就少。像 `drawer-47` 那种「隔板横跨整行 + 两个分区」的盘面，分区把最小可用棋盘顶到 7×6（可用 35 格），而原版只让放 4 件——四件旧物最多二十四五格，怎么凑都差得远。解法是把 `keepCount` 也当成未知量：对每块装得下的抽屉反解「挑任意几件恰好凑够」，要求**至少 6 种挑法能通关**，实测多数在 20 种以上。
3. **物品栏 18 件要在画布上真的放得下。** 六列封顶，三行时格子压到 68px 高、图标缩到 38px，靠缩图标把第三行的空间让出来（`400 + 2×76 + 68 = 620 ≤ 640`）。几何口径同步进了 `tests/validate-geometry.cjs`。

**激励视频是这一轮新接的能力。** 「超出免费额度的一点提示」与「消除一件旧物」都挂在广告上，适配层独立成 `ads.js`（约 80 行）：

- 严格照官方口径：**只有 `onClose(res => res.isEnded)` 为真才发奖励**，不能 `show()` 一返回就发。
- `AD_UNIT_ID` 留空即视为未接入，直接放行。网页版、WebView 版、APK 都能照常玩——不会出现「点了没反应」。
- 「消除一件」在播广告**之前**就算好要消哪件，并用求解器验过「消掉之后这一关仍可解」，不让玩家看完广告才被告知消不得。
- 遗留：TapTap「H5 快速上架」（WebView 承载）里有没有 `tap` 全局还没确证，官方文档目前只覆盖 JS VM 小游戏。

## 1.0.4 补：收纳工作台皮肤（6 款）

需求是「设置几款收纳工作台的皮肤，名字都要取得响亮霸气一点」，以及「想一下加什么机制和内容能让玩家看广告」。前一条做进了游戏，后一条单独写成了 `AD_PLAN.md`。

**皮肤做成了「调色板派生」，不是另一套画法。** 一款皮肤只提供十个种子色（底色、台面、台沿、木框、木纹、隔板、分区、物品格底、丝绒、金属），其余四十来个色值（格线、木纹环、图钉、阴影、提示色、物品台的明暗）由亮度关系算出来。好处是加一款皮肤的成本是「填十个色」而不是「画一遍」，也不会出现「换了台面忘了换隔板」的半拉子皮肤。

| 皮肤 | 别名 | 解锁 | 看广告 |
|---|---|---|---|
| 栖湾原木台 | 试营业的第一张台子 | 默认 | 不接（本来就免费） |
| 玄铁九鼎案 | 寒铁冷银，落子生风 | 整理完 10 份委托 | 可解锁 |
| 赤霄朱漆台 | 朱漆描金，一寸不让 | 整理完 25 份委托 | 可解锁 |
| 青玉藏龙案 | 青玉为面，龙纹在底 | 整理完 45 份委托 | 可解锁 |
| 鎏金万象案 | 墨底鎏金，万物归位 | 整理完 70 份委托 | 可解锁 |
| 长夜星砂台 | 攒满十八段回忆才亮得起来 | 回忆图鉴 18 / 24 段 | **不接** |

三个关键判断：

1. **默认皮肤必须逐色写死，不能靠派生。** 派生函数以后加一个新键，默认款就会跟着变一点——「默认 = 改版前的观感」这件事就不再是结构保证，而是「碰巧算对」。所以默认款另有一张 `OAK` 表把每个色值逐个钉住，并配了一条**结构性断言**：派生出来的键，只要默认款没写死，测试就红。
2. **皮肤不许碰判定。** 皮肤只动画布与导出作品图的配色，`computeLayout` / `scoreLayout` / 任何判定一个字不改。真浏览器验收里专门切三款比对摆法、整齐度、通关状态是否逐项不变——这既是质量要求，也是「看广告不会变强」这句话的凭据。
3. **两条解锁路并行。** 条件解锁（通关数 / 图鉴段数）是**推出来的、不落盘**，老存档天然兼容；只有看广告解锁才写进 `saved.skins`。两款刻意 `adUnlock:false`：默认那款本来就免费，星砂台是收集品——**收集品不能什么都能买**。

**合规沿用同一把尺子。** 皮肤面板的按钮和别的广告位问的是同一个 `adsReady()`（`ads.js` 的 `AD_UNIT_ID` 非空且拿得到宿主接口）。拿不到广告位时按钮写「还不能拿」而不是「看广告解锁」——否则就和 `docs/privacy.html` 里「当前没有接入任何广告 SDK」那句对不上了。单测与真浏览器双端都断言了网页版只写「还不能拿」。

## 下一轮的候选

现在缺的仍然不是内容量，是**一次真人试玩**。但本轮离线体检暴露了一个比“缺试玩”更具体的问题，所以它排在最前面。

| 优先级 | 项目 | 为什么现在做 | 依赖 | 验收 | 成本估计 |
|---|---|---|---|---|---|
| **P0** | **整齐度按关卡自身上限归一化** | 见下节：93 关里有 39 关，随手摆和认真摆只差不到 5 分，评语几乎不动 | 无 | 每关的评语分布铺开；「最整齐」在每关都够得到且要费点劲 | 开发 2 人日 |
| P0 | 20–30 人观察试玩 | 所有待定判断都卡在这 | 可玩的 0.4.0 | 独立完成率、误操作位置、是否有人主动为了分数重摆、是否有人去找图鉴 | 策划／测试 3 人日 |
| P1 | 每日委托（方案3）：从已验证题库里抽一份带种子的选做委托 | 复用 100 关，不新增关卡成本就能造回访理由 | P0 确认玩家愿意重摆 | 种子可复现、全部可解、不影响主线、提前备好 14 天题 | 开发 3–4 + 策划 3–4 人日 |
| P1 | 出题挑战码（方案4）：把某次摆法编码成短码发给别人复现 | 直接服务可分享性，且比截图更能传“怎么摆” | 整齐度口径稳定（即上面的 P0） | 同一码还原同一局面；对方只能看到目标不能直接抄答案 | 开发 2–3 人日 |
| P2 | 作品卡标注本次保留的物品与图鉴解锁 | 取舍关的差别现在只在场上，导出后看不出来 | 出题码或每日委托任一 | 导出内容与玩家作品一致 | 开发／美术 1–2 人日 |
| P2 | 背景音乐加音量滑杆，并备第二首 | 现在只有开关没有音量，且 32 秒循环迟早会腻。BGM 已经是独立模块（`music.js`），换曲只是换两张表 | 无 | 音量可调且记进存档；两首可切换；仍然不引入音频文件 | 开发 1–2 人日 |

广告该怎么加，本轮单独落成了一份 `AD_PLAN.md`，不挤在这张表里——因为它回答的不是「加什么功能」，而是「加在哪儿才不惹人烦」。三条硬原则：**只加想要不加需要**（100 关全程免费）、**先算后播**（算不出方案就不播广告）、**拿不到广告就放行**（并且按钮不写「看广告」三个字）。现有四个位（超额度的一点提示 / 消除一件 / 皮肤解锁 / 网页版直接放行）之外，候选方向是外观（作品图相框、委托信用纸配火漆）、内容（「店主说书」图鉴补全，每天一条）、仪式（今日一签）。判断线很具体：**外观类的完播率应当显著高于救援类**——如果反了，说明外观做得不好看，该改美术而不是加位。明确不做：体力、计时器、锁关、变强、插屏、抽奖。

### P0 详情：整齐度为什么需要归一化

`tools/gradient.cjs` 把「朴素摆法」（求解器的左上贪心解，等价于玩家随手把东西塞进去）和「认真摆过的最好分数」放在一起比：

| 范围 | 朴素分中位 | 最好分中位 | 梯度中位 | 梯度 < 5 的关卡 |
|---|---|---|---|---|
| 第一辑（43 关非教学） | 90 | 97 | 6 | 19 |
| 第二辑（50 关） | 89 | 98 | 9 | 20 |
| 全部（93 关） | 90 | 97 | 7 | 39 |

也就是说，**近四成的关卡里，玩家随手一摆就已经接近满分**，再摆也不会听到不同的话。原因是评语用的是绝对分（82 / 74 / 62 三档），而各关可达上限本来就不同：抽屉格少、形状规整的关卡，随手塞进去就已经在「对齐、留白、居中」三项上都接近满分。

现有实现只能把评语线压到所有关卡的下限之下（实测最低上限 84，所以最低那档线定 82），代价就是大部分关卡永远落在最高那档。改法有两条，建议走第一条：

1. **归一化（推荐）**：离线用 `tools/ceiling.cjs` 给每关算出可达上限，把表像 `HARDEN` 一样写进 `core.js`，评语按 `本次分数 / 该关上限` 分档。这样每关都能听到从“先摆开”到“挑不出毛病”的完整四档，而玩家的目标也从“拿到 90 分”变成“把这一关摆到最好”。
2. 重调权重让分差自然拉大。风险更高：评分权重一改，`polish.test.cjs` 里的上限不变量、评语线和 `tools/*.cjs` 的基线全部要重测，而且不保证能把 39 关都救回来。

## 明确不做的

- **不再单纯加关卡。** 100 关之后加关只增加同一种体验；要么加新规则，要么加同一关里的新目标。
- **不做实时联机、排行榜、PVP。** 玩法允许多解，还没有稳定的公平评分，服务器与反作弊成本也高。出题码是异步挑战，先走这条。
- **不把付费提前，也不把广告从「想要」挪到「需要」。** 免费提示、撤销、取回物品保持免费；100 关全程不需要看任何一次广告。看广告只换两样东西：外观（皮肤），以及已经免费给过的额度（超额度的一点提示 / 消除一件旧物）。不换通关条件、不换难度、不换分数。**外观类广告位已落地**（见上节与 `AD_PLAN.md`）；付费（一次性买断某款外观）仍然不立项——要等每日委托证明有回访。
- **不做无限制程序随机关卡。** 难度、可解性和提示成本难控；每日委托只从已验证题库里抽。

## 试玩要看什么

不是看“能不能通关”——100 关都验证过可解。要看的是：

1. 第一次拿到整齐度分数后，有多少人**主动重摆**同一关？这是这套评分成不成立的唯一标准。归一化就是为了让这条更容易成立。
2. 有没有人注意到旧物提示条，并因此在下一关主动把两件东西摆到一起？
3. 新手章节之后，玩家是接着玩自己刚认过的那一章，还是跳到别的章节？后者说明分组导航有用。
4. 有没有人玩到第二辑还察觉不出难度在哪儿——如果有，说明“靠形状和条件加难”这条路走到头了，该上新机制。

方向性目标：首次接触新规则的玩家中，至少 80% 能不经口头讲解完成新手章节对应那一关；至少 40% 在拿到分数后至少重摆一次。目标不是实测结果，未达到时先改规则提示与评分反馈，而不是继续加内容。

100 关仍不足以证明 D7／D30。只有确认“重摆意愿”这一条成立，再谈题库、每日委托和商业入口。
