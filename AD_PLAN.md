# 激励视频：加什么机制、加什么内容，玩家才愿意看

**状态：** 提案（2026-09-14）。第一部分「工作台皮肤」已经落地，其余是排好序的候选。
**前提：** 这是款慢节奏的收纳解谜，用户一局可能坐十分钟。它的广告不能用「打断」换曝光，
只能用「玩家自己想要的额外东西」换主动点击。

---

## 一、先说立场：三条硬原则

1. **只加想要，不加需要。** 广告不许出现在「不看完就玩不下去」的地方。
   100 关必须全程免费、全部可解；整齐度、判定、提示上限都不因看没看广告而变。
2. **先算后播。** 要发的东西必须先算出来再播——算不出来就不播。
   现在「提示／代放／消除」三个按钮都已经是这个结构（见 `game.js` 的 `planHint/planAuto/clearCandidate`）。
   玩家看完广告才被告知「这一步没法替你放」，是这类广告最常见、也最伤人的毛病。
3. **拿不到就放行。** 网页版、广告位没配、拉取失败——一律直接给，并且**不许写「看广告」三个字**。
   游戏内的隐私政策写着「当前没有接入任何广告 SDK」，按钮喊看广告就和它对不上了
   （TapTap 的口径是「政策须真实有效、与 App 内协议一致」）。这条现在是自动守的：
   按钮文案与皮肤面板都问同一个 `adsReady()`。

> 一句话：**广告是本店多出来的一项服务，不是进门的门票。**

---

## 二、现在有几个位、各自的上限

| # | 位置 | 给什么 | 上限 | 状态 |
|---|---|---|---|---|
| 1 | 一点提示 | 指出下一件放哪、什么方向；摆错了直接点名是哪一件挡路 | 每关免费 1 次，之后接广告 | 已上线 |
| 2 | 帮我放一件 | 由店里替你放好一件（附送把放错的那件收回来） | 每关 1 次 | 已上线 |
| 3 | 消除一件 | 把一件不必须、也没被固定的旧物从这份委托里拿掉 | 每关最多 2 次 | 已上线 |
| 4 | 工作台皮肤 | 6 张台子，条件解锁或看广告解锁 | 每款一次，永久 | **本轮新增** |

第 1–3 个是「省事」，第 4 个是「好看」。**这两类的广告价值完全不同**——
省事是止痛药，好看是糖。止痛药的效果随玩家水平下降（越熟练越不需要提示），
糖的效果相反（越投入越想换台子）。所以第 4 类要成体系地做，下面第三节就是。

---

## 三、要加的内容：四个方向，按「离玩家多近」排序

### 方向一：外观（已开一类，还能再开三小类）

皮肤换的是「工作台」，一局里从头看到尾，是最高频的可视面积。同一条思路还能沿下去：

| 候选 | 换什么 | 为什么它比皮肤更容易被接受 | 成本 |
|---|---|---|---|
| **作品图相框** | 导出 PNG 的边框与角花 | 导出图是**要发出去**的东西。人愿意为了「发出去好看」看广告，这是全店最自然的一位 | 小（改 `savePicture`） |
| **委托信用纸 + 火漆印章** | 左侧委托信的纸纹、印色 | 每关都读一遍，出现频率比皮肤还高 | 中（CSS + 一小段 SVG） |
| **旧物箱格子样式** | 物品栏卡片的纹理 | 一局里点最多的区域 | 小 |

外观类的分寸：**默认那款永远免费**，且**至少留一款不接广告**（现在留了「长夜星砂台」，
只能靠攒满回忆图鉴拿）。理由是收集品一旦「什么都能买」，收集本身就没意义了，
剩下的广告价值会一起垮掉。

### 方向二：内容（力度最大，也最该做）

**「店主说书」——图鉴的补全。**

回忆图鉴现在是 24 段，靠把两件旧物摆到一起解锁。差两三段没解锁的玩家，
**痛感是最强的**——收集系统的缺口永远比新内容更诱人。

- 做法：图鉴面板里未解锁的卡片给一个「听店主讲一段」的入口，看一段广告直接解锁该条。
- 上限：**每天最多 1 条**（按自然日冷却，写进存档）。
- 为什么加冷却：不冷却就是「花钱集齐」，图鉴立刻贬值；冷却之后它变成
  「今天这一条我特别想知道」，看完反而更珍惜。
- 额外的好处：它给「回来」一个理由。24 段按一天一条，够撑三周。
- 成本：小。图鉴面板、解锁逻辑、存档字段都有了，只是多一个入口。

**节气电台——第二轨环境音。**

100 关里埋了春→冬的线索（回南天、落雨不停、蝉声、长昼、搬家季…），
但现在只有一首 32 秒的 BGM，季节是「读」出来的，不是「听」出来的。

- 做法：看广告解锁第二条环境音轨——雨声 / 蝉声 / 炭火声（都在 `music.js` 里现场合成噪声，
  **不引入音频文件**，保持零体积）。
- 为什么它适合挂在广告上：纯氛围、纯体验、完全不碰难度，而且和游戏的世界观咬得很紧。
- 成本：小到中（`music.js` 已经是独立模块，多两张表 + 一层混音）。

### 方向三：仪式（低频，换回访）

**「今日一签」**：每天第一次打开，看一段广告，得一句店主手写的话 + 一张旧物明信片。

- 上限：一天一次，点了才有，绝不自动弹。
- 它不改变任何玩法，是纯赠品。适合放在结算面板之后，
  玩家刚完成一份委托、心情最好的那一刻。
- 明信片可以攒成一册——这又是一条收藏线，成本比图鉴低（一张卡片就是一段文字 + Canvas 小画）。

### 方向四：省事的延伸（最后一个，慎用）

现在三个救援按钮都是「每关固定额度」。唯一还值得加的是一次**跨关的合计额度**：
「每关一次免费提示」改成「每天 3 次免费提示，用完接广告」。

- 好处：把「这一关我卡住了」和「今天我不想看广告」这两个不同的问题分开。
- 风险：额度是玩家能感知到的减法。做之前必须先确认**有人会连续卡住**，
  否则只是把一件本来免费的事变得要算账。
- 建议：先不做。等方向二的恢复率数据出来再说。

---

## 四、防骚扰：把「节制」写成可验收的规则

| 规则 | 具体值 | 为什么 |
|---|---|---|
| 只在玩家主动点击后播放 | 无例外 | 插屏、开屏、自动弹，一律不做。这个游戏的气质不允许 |
| 单次会话请求上限 | 建议 8 次 | 超过就说明体验设计出了问题，不是玩家贪心 |
| 同一动作重复请求间隔 | ≥ 30 秒 | 防止误触连点造成「刚看完又要点」 |
| 每关救援类总额度 | 提示 1（免费）+ 代放 1 + 消除 2 | 保留现有值，别涨。涨额度等于承认关卡难 |
| 内容类（说书）冷却 | 每天 1 条 | 见上，保护收集的稀缺性 |
| 不看不亏 | 不看广告的玩家不该看到任何「你错过了」 | 不许出现红点、倒计时、「限时」「只剩」 |
| 拉不到就放行 | 三条 `STRICT=false`，变现跑通前不翻 | 广告没拉到是我们的问题，不该罚玩家 |

---

## 五、怎么知道做对了（先定判断线，再看数）

埋点已经跟着广告链路走：`ad_request` / `ad_rewarded` / `ad_dismissed` / `ad_bypass`
（都在 `saved.events` 里，带 `for` 字段标明是哪一位）。加上原有的
`hint` / `item_auto_placed` / `item_cleared` / `level_complete`，能算出四件事：

| 看什么 | 怎么算 | 判断线（先立好，免得事后找理由） |
|---|---|---|
| 完播率 | `ad_rewarded / ad_request` | < 60% → 说明**价值不够**，加价值，不要加频次 |
| 各位的占比 | 按 `for` 分组 | 某一位请求 < 5% → 位置太隐蔽或价值太低，**撤掉**，别加提示 |
| 看完之后干了什么 | `ad_rewarded` 后 5 分钟内的 `item_placed` / `level_complete` | 看完什么都不做 → 这一位给的东西没用 |
| 卡点分布 | `hint` / `item_cleared` 集中在哪几关 | 集中在少数关卡 → **该改关卡**，不是该加广告 |

还有一条结构性预期，用来验证「想要 vs 需要」这个模型成不成立：

> **外观类的完播率应当显著高于救援类。**
> 如果反过来了，说明救援类的痛感被设计得过强（关卡太难、或提示给得太少），
> 那是在用难度逼广告——这条路必须掉头。

---

## 六、明确不做

- **不做体力／耐心值／倒计时。** 慢节奏游戏加计时器等于换了一个游戏。
- **不把任何一关锁在广告后面。** 100 关全程免费可解，这是产品的地基。
- **不做「看广告变强」。** 提示上限、整齐度权重、关卡判定，一律不因广告改变。
  看广告换到的只能是**好看**和**省事**。
- **不做插屏、开屏、横幅。**
- **不做「失败→看广告复活」的循环。** 那需要先制造失败，属于拿玩家当对手。
- **不做抽奖／转盘／随机奖励。** 和「不制造焦虑」的调性冲突，也容易被当成诱导。

---

## 七、落地顺序

| 顺序 | 项目 | 依赖 | 验收 | 成本 |
|---|---|---|---|---|
| **P0** | 工作台皮肤（6 款） | 无 | 已落地：真浏览器 27 项 + 单测 10 项 | — |
| **P0** | 「店主说书」图鉴补全（每日 1 条） | 图鉴面板已存在 | 同一段不会被买两次；冷却按自然日；不看广告也能靠摆出来解锁 | 小 |
| P1 | 作品图相框 | 皮肤调色板（已有 `pic*` 一组色值可直接复用） | 导出的 PNG 与所选相框一致 | 小 |
| P1 | 节气电台（第二轨环境音） | `music.js` | 假 AudioContext 核音符／噪声排布；真浏览器确认音频图建得起来 | 中 |
| P2 | 每日委托 + 今日一签 | 需要先有一份「每日一题」的题库 | 种子可复现、全部可解、不影响主线 | 中 |
| P2 | 委托信用纸 + 火漆印章 | 无 | 六款信纸的对比截图 | 中 |

---

## 八、合规这件事必须同一轮做完

三个位置（`index.html` 的隐私弹窗、`docs/privacy.html`、`docs/privacy.md`）现在都写着
「当前没有接入任何广告 SDK」——**这句话在 `AD_UNIT_ID` 为空时是真的**，所以本轮只加了
「皮肤可以看广告换」的说法，没有动那三句。等广告位真正下来，必须同一轮里：

1. 用 `check_ads_status` 取服务端下发的广告位 ID（**不许手抄、不许跨应用借用**）；
2. 改上面三处文案（`tools/set-ad-unit.cjs` 会在写 ID 前检查，没改到位就拒绝写入）；
3. `node tools/set-ad-unit.cjs <space_id>`；
4. 跑全量单测 + 真浏览器那几支验收，再打包。

另外一个卡点还挂着：广告位要按横竖屏匹配，而本应用不是「关卡类型」，
服务端接口一律 400 拒绝——**屏幕方向只能在 TapTap 开发者后台人工设置成竖屏**。
这件事没做完，广告位就拿不到，第三节的内容也就都还只能按「未接入」发版
（免费放行、按钮不写「看广告」）。这不是代码问题，是平台侧的人工步骤。
