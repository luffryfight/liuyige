// 一次性文案修正：把取舍关信件与标题里写死的件数改掉。
//
// 为什么需要这一步：tools/nearfull.cjs 会把 keepCount 按盘面需要上调（40 关都动了），
// 而信件当初是按原始配额写的——「另外三件你替我挑」在配额变成八件之后就自相矛盾了。
// 规则卡（levelRule）本来就会写「从 N 件中选留 K 件」，叙述里再报一次数字纯属重复，
// 而且一改配额就会过期。所以统一改成不依赖数字的说法。
//
// 用法：node tools/fix-letters.cjs --check   # 只看会改哪些，不落盘
//       node tools/fix-letters.cjs           # 真的改
const fs = require('node:fs');
const path = require('node:path');

const FILE = path.join(__dirname, '..', 'core.js');
const check = process.argv.includes('--check');

// [旧, 新]。旧片段必须原样出现在 core.js 里，否则报错——宁可失败也不要静默漏改。
const PAIRS = [
  // ---- 标题 ----
  ['搬家留三样', '搬家要留的'],
  ['顺手的两件', '顺手带上的'],
  ['春天剩下的三件', '春天剩下的'],
  ['夏天的三件', '夏天要留的'],
  ['秋天最后三件', '秋天最后收的'],
  ['暑假只带四件', '暑假随身的'],
  ['秋天带走五件', '秋天带走的'],
  ['冬天只留五件', '冬天留下的'],

  // ---- 信件 ----
  ['行李里只留四件。合照一定要带，另外三件由你挑', '行李里只留几件。合照一定要带，其余的你挑'],
  ['再选两件放进箱子。耳机、旧书、相片与来信，你会带哪两件？', '再挑几件放进箱子。耳机、旧书、相片与来信，你会带哪些？'],
  ['再挑三件常用物。', '再挑几件常用物。'],
  ['相片与店钥匙必须留下，再选三件。', '相片与店钥匙必须留下，再挑几件。'],
  ['未选的那件先留在桌面', '没选上的先留在桌面'],
  ['杯子一定要留，另外三件你来定', '杯子一定要留，其余的你定'],
  ['剩下两件随手挑', '剩下的随手挑'],
  ['另外四件从剩下的里面挑', '其余的从剩下的里面挑'],
  ['其余三件你自己挑。', '其余的你自己挑。'],
  ['剩下三件，从桌面上挑。', '剩下的，从桌面上挑。'],
  ['剩下三件从桌面上挑。', '剩下的从桌面上挑。'],
  ['再挑三件一起带走。', '再挑几件一起带走。'],
  ['另外四件你挑；', '其余的挑几件；'],
  ['再选四件一起带走。', '再挑几件一起带走。'],
  ['再挑三件。', '再挑几件。'],
  ['再挑四件。', '再挑几件。'],
  ['另外三件你替我挑。', '其余的由你替我挑。'],
  ['再挑四件一起收。', '再挑几件一起收。'],
  ['这次能带五件。相片一定要带，其余四件你挑。', '这次能带的有限。相片一定要带，其余的你挑。'],
  ['另外三件你挑。', '其余的挑几件。'],
  ['另外三件来不及细想，你替我定。', '其余的来不及细想，你替我定。'],
  ['能带五件。耳机一定要带，其余四件你决定。', '能带走的有限。耳机一定要带，其余的你决定。'],
  ['只留五件。杯子和钥匙都要，其余三件你定。', '东西不多，留下的要精。杯子和钥匙都要，其余的你定。'],
  ['剩下三件从桌面里挑。', '剩下的从桌面里挑。'],

  // ---- 回信 ----
  ['其余三件正好陪着这个下午。', '其余几件正好陪着这个下午。'],
  ['耳机垫在最下面，上面的四件都稳稳当当。', '耳机垫在最下面，上面的几件都稳稳当当。'],
  ['一块木扣，一张相片，其余四件都找到了自己的位置。', '一块木扣，一张相片，其余几件都找到了自己的位置。'],
  ['杯子放在最外层，剩下三件也都有了去处。', '杯子放在最外层，其余几件也都有了去处。'],
  ['木箱归位，另外四件也都找到了边角。', '木箱归位，其余几件也都找到了边角。'],
  ['相片压在最下面，上面四件都稳稳的。', '相片压在最下面，上面的几件都稳稳的。'],
  ['手边区收下了两件常用的，其余三件在上层排开。', '手边区收下了常用的几件，其余的在上层排开。'],
  ['读本区装了两个本子，剩下三件在上层等着。', '读本区装下了本子，其余的在上层等着。'],
  ['钥匙搁在最外层，其余三件也塞得整整齐齐。', '钥匙搁在最外层，其余的也塞得整整齐齐。'],
  ['耳机垫在最下面，上面四件稳稳当当。', '耳机垫在最下面，上面的几件稳稳当当。'],
  ['影像区收下了要收的，剩下的三件也各就各位。', '影像区收下了要收的，剩下的也各就各位。'],
  ['杯子和钥匙在外侧，三件薄的填在里面。', '杯子和钥匙在外侧，薄的填在里面。'],
];

let src = fs.readFileSync(FILE, 'utf8');
let hits = 0, misses = [];
for (const [from, to] of PAIRS) {
  if (!src.includes(from)) { misses.push(from); continue; }
  const n = src.split(from).length - 1;
  hits += n;
  if (!check) src = src.split(from).join(to);
  console.log(`  ${String(n).padStart(2)} 处  ${from}`);
}
if (misses.length) {
  console.error('\n下面这些片段在 core.js 里找不到，请核对原文：');
  for (const m of misses) console.error('  ! ' + m);
  process.exitCode = 1;
}
if (check) { console.log(`\n--check：共会改 ${hits} 处，未落盘。`); process.exit(process.exitCode || 0); }
fs.writeFileSync(FILE, src);
console.log(`\n已改 ${hits} 处；找不到 ${misses.length} 处。`);
